# Progress

import { Bento, Stack, Text, Input, LucideIcon, Progress, Display } from "ui";

<Display label="progress">
  <Progress tone="tomato" />
  <Progress tone="sky" />
  <Progress value={50} max={100} leftLabel="hello" rightLabel="world" />
</Display>
