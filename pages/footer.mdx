import { Box, Footer, Display, stitchesDarkTheme } from "ui";

# Footer

The official footer compoenet. Use `isDark` to change the theme if used as a standalone component.

<Display label="footer">
  <Footer />
</Display>
