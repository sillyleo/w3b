import React from "react";
import Bento, { BentoProps } from "../Bento";

export interface SwitchProps extends BentoProps {
}

const Switch = ({...props}: SwitchProps) => <Bento/>;

export default Switch;