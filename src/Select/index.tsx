import React from "react";
import Bento, { BentoProps } from "../Bento";

export interface SelectProps extends BentoProps {
}

const Select = ({...props}: SelectProps) => <Bento/>;

export default Select;