import React from "react";
import Bento, { BentoProps } from "../Bento";

export interface RadioGroupProps extends BentoProps {
}

const RadioGroup = ({...props}: RadioGroupProps) => <Bento/>;

export default RadioGroup;